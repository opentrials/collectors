# Prepare
# scrape all data from register

actrn:
  image: okibot/collectors
  command: make start actrn 2001-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

euctr:
  image: okibot/collectors
  command: make start euctr 2001-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

fda:
  image: okibot/collectors
  command: make start fda
  environment:
    WAREHOUSE_URL:
    LOGGING_URL:

gsk:
  image: okibot/collectors
  command: make start gsk 2001-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

icdcm:
  image: okibot/collectors
  command: make start icdcm
  environment:
    WAREHOUSE_URL:
    LOGGING_URL:

icdpcs:
  image: okibot/collectors
  command: make start icdpcs
  environment:
    WAREHOUSE_URL:
    LOGGING_URL:

ictrp:
  image: okibot/collectors
  command: make start ictrp
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:
    ICTRP_USER:
    ICTRP_PASS:

isrctn:
  image: okibot/collectors
  command: make start isrctn 2001-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

jprn:
  image: okibot/collectors
  command: make start jprn 1 100000
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

nct:
  image: okibot/collectors
  command: make start nct 2001-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

pfizer:
  image: okibot/collectors
  command: make start pfizer
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

pubmed:
  image: okibot/collectors
  command: make start pubmed 1900-01-01 2100-01-01
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:

takeda:
  image: okibot/collectors
  command: make start takeda
  environment:
    DOWNLOAD_DELAY: 1
    WAREHOUSE_URL:
    LOGGING_URL:
