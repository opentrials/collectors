# Update
# scrape only recently updated trials

actrn:
  image: okibot/collectors
  command: scrapy crawl actrn -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

euctr:
  image: okibot/collectors
  command: scrapy crawl euctr -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

gsk:
  image: okibot/collectors
  command: scrapy crawl gsk -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

ictrp:
  image: okibot/collectors
  command: scrapy crawl ictrp -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10
    OPENTRIALS_ICTRP_USER:
    OPENTRIALS_ICTRP_PASS:

isrctn:
  image: okibot/collectors
  command: scrapy crawl isrctn -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

jprn:
  image: okibot/collectors
  command: scrapy crawl jprn -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

nct:
  image: okibot/collectors
  command: scrapy crawl nct -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

pfizer:
  image: okibot/collectors
  command: scrapy crawl pfizer -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10

takeda:
  image: okibot/collectors
  command: scrapy crawl takeda -L INFO
  restart: always
  environment:
    OPENTRIALS_WAREHOUSE_URL:
    OPENTRIALS_DOWNLOAD_DELAY: 10
